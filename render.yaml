services:
  - type: web
    name: we-got-issues
    runtime: python
    runtimeVersion: 3.13
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.13
